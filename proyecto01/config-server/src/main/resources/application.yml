server:
  port: 8085
spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/caitoVilas/curso-microservicios-kafka
          clone-on-start: true
